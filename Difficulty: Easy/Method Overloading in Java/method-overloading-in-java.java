class Calculator {

    // Sum of two integers
    int sum(int a, int b) {
        return a + b;
    }

    // Sum of three integers
    int sum(int a, int b, int c) {
        return a + b + c;
    }

    // Sum of two double values
    double sum(double x, double y) {
        return x + y;
    }
}